##webserver
sudo apt-get update 
sudo apt install software-properties-common
sudo apt-add-repository ppa:ansible/ansible
sudo apt-get update
sudo apt-get install ansible
ansible_ssh_private_key_file=swarajansible02 local host = ssh
ansible_connection=local

#connect through ssh
ansible_ssh_private_key_file=/etc/ansible/swaraj66.pem ansible_connection=ssh

#changes to do
deprecation_warnings=False


#inventoryfile
[server name]
ip address (sonnevction through ssh)
ip address ansible_ssh_private_key_file=/etc/ansible/swaraj66.pem ansible_connection=ssh
